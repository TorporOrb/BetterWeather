<nav class="navbar navbar-expand bg-dark border-bottom border-body mb-5 fixed-top" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_homepage') }}">BetterWeather</a>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ms-auto"> <!-- Align items to the right -->
                <li class="nav-item dropdown dropright"> <!-- Use dropright class for right-aligned dropdown -->
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Cities
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        {% for cityName in get_city_names() %}
                          <li><a class="dropdown-item" href="{{ path('app_location', {city: cityName} ) }}">{{ cityName }}</a></li>
                        {% endfor %}
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>