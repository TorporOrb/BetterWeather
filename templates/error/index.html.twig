{% extends 'layout_boxed.html.twig' %}

{% block content %}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8 mt-5">
            <div class="card mt-5">
                <h2 class="card-header p-5">{{ "Something went wrong" | trans | trans }}</h2>

                <div class="card-body p-5">
                    <h4>{{ "Check your url for typo's and check back with us." | trans }}</h4>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}